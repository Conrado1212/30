const express = require("express");
const port = 3000;
const app = express();

const dynamic ={};

const endpoints =[];

let id = 0;

app.post('/new', (res,req)=>{


 
})






app.listen(port, ()=>{
    console.log(`App working at http://localhost:${port}`);
})